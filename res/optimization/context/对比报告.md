# 上下文优化方案对比报告

> 生成时间：2026-08-20 15:28:10

## 1. 实验概述

- **评测集**：30 个问答对（覆盖 16 个新能源汽车标准）
- **检索基线**：HyPE + BM25 + RRF 重排（上一步最优），top_k=5
- **上下文优化**：四种方案均在检索结果之上、送 LLM 生成之前做后处理
- **C1 RSE**：把连续位置的相关块合并成段，补上被夹在中间的块
- **C2 窗口增强**：为每个检索块加入前后相邻块（radius=1）
- **C3 上下文压缩**：LLM 提取每个块中与问题相关的部分，剔除无关块
- **C4 U型排序**：最高分放开头、次高分放结尾，缓解 lost-in-the-middle
- **评测**：ragas；Embedding：bge-large-zh-v1.5

## 2. 平均指标对比

| 方案 | 忠实度 | 答案相关性 | 上下文精确率 | 上下文召回率 |
|---|---|---|---|---|
| R0 基线 混合+RRF | 0.9544 | 0.8512 | 0.8150 | 0.9667 |
| C1 RSE 段提取 | 0.9509 | 0.8643 | 0.7655 | 0.9500 |
| C2 上下文窗口增强 | 0.9458 | 0.8542 | 0.6873 | 1.0000 |
| C3 上下文压缩 | 0.6894 | 0.7063 | 0.9380 | 0.8667 |
| C4 U型排序 | 0.9368 | 0.8608 | 0.7769 | 0.9667 |

## 3. 与基线（R0）对比

- **C1 RSE 段提取** vs R0：忠实度▼0.004 / 相关性▲0.013 / 精确率▼0.049 / 召回率▼0.017
- **C2 上下文窗口增强** vs R0：忠实度▼0.009 / 相关性▲0.003 / 精确率▼0.128 / 召回率▲0.033
- **C3 上下文压缩** vs R0：忠实度▼0.265 / 相关性▼0.145 / 精确率▲0.123 / 召回率▼0.100
- **C4 U型排序** vs R0：忠实度▼0.018 / 相关性▲0.010 / 精确率▼0.038 / 召回率=0.000

## 4. 逐指标最优方案

| 指标 | 最优方案 | 数值 |
|---|---|---|
| 忠实度 Faithfulness | R0 基线 混合+RRF | 0.9544 |
| 答案相关性 Answer Relevancy | C1 RSE 段提取 | 0.8643 |
| 上下文精确率 Context Precision | C3 上下文压缩 | 0.9380 |
| 上下文召回率 Context Recall | C2 上下文窗口增强 | 1.0000 |

## 5. 结论

- **基线（混合+RRF）仍是最均衡方案**：忠实度 0.9544 为最高，其余指标也都较强。4 种上下文优化大多是在指标间做取舍，没有一种全面超越。
- **U型排序（C4）是安全且零成本的增益**：相关性 +0.010，其余基本持平，无需额外模型/LLM 开销。作为默认上下文排列方式推荐。
- **RSE 段提取（C1）相关性最优**（0.8643，+0.013），但精确率/召回率略降；适合更关注答案相关性的场景。
- **窗口增强（C2）召回率拉满**（1.0000，+0.033），但加入邻居带来噪音使精确率明显下降（-0.13，且该指标有 11/30 缺失、结果不可靠）；适合召回优先且能容忍噪音的场景。
- **上下文压缩（C3）高精确率但忠实度崩塌**：精确率 0.9380（+0.123）为最优，但压缩去掉了支撑信息，忠实度暴跌至 0.6894（-0.265）——压缩后的答案失去原文依据，不推荐当前实现。
- **最终建议**：检索层保持 HyPE+BM25+RRF；上下文层默认采用 **U型排序**；若追求答案相关性可叠加 RSE；压缩需改进提示词（保留关键数值与限定）后再评估。

## 6. 逐题明细

见 `res/optimization/context/eval_*.csv`。